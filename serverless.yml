service: ${env:GITHUB_REPO_NAME}

custom:
  PROJECT: Cartography by Zlotin

provider: ${file(./serverless-provider.yml)}

functions:
  writer:
    handler: handler.writer
    fileSystemConfig: ${file(./serverless-fs.yml)}
    events:
      - schedule: rate(6 hours)
  reader:
    handler: handler.reader
    fileSystemConfig: ${file(./serverless-fs.yml)}
    events:
      - http:
          path: read/{param}
          method: get
          cors: true
